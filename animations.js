gsap.registerPlugin(scrollTrigger);
gsap.to("cards", {opacity:1, delay:0.1})
let iteration =0;
const spacing = 0.1;
snpa = gsap.utensils



function showText() {
  // Find the text element by its ID and change its display style to make it visible
  document.getElementById("message").style.display = "block";
}


